ifdef LONG
INTT = -DLONG
endif

PCC = g++
PCFLAGS = -O2 -fopenmp -fcilkplus $(INTT)
PLFLAGS = -fopenmp -lcilkrts -L ../../../runtime -lrt -lm -lclustering $(LFLAGS)
MANAGER = ../../../runtime/task_manager.o
#MANAGER = ../../../runtime/utilization_calculator.o

ADDITIONAL_LINKS = $(MANAGER) ../../../runtime/single_use_barrier.o ../../../runtime/timespec_functions.o -Wl,-rpath,$(HOME)/cilkplus-install/lib64
